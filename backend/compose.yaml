# Credenciais públicas apenas para desenvolvimento local. Não reutilizar em produção.
services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=omnicash_test'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5433:5432'
